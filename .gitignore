# Windows Explorer metadata
desktop.ini

# LibreOffice/Office lock files
.~lock.*#

# Redundant archives (source files already extracted in "reagenet & MSDS/reagent and MSDS/")
reagenet & MSDS/reagent and MSDS.rar
reagenet & MSDS/reagent and MSDS.zip

# Local JSON backups exported by FCP68030 form on failed/manual sync
maintanance/6800/FCP68030_*.json

# Cloudflare Wrangler local cache/build output — never publish, never commit
.wrangler/
